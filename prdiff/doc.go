// Package prdiff provides a function to get the diff of a pull request using GitHub API or git command.
// It first tries to get the diff via the GitHub API, and falls back to git command if it fails.
package prdiff
